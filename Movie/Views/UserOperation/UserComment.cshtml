@model IEnumerable<Movie.Models.CommentDetail>
@{
    Layout = "~/Views/Shared/_User.cshtml";
}
@{
    ViewBag.Title = "用户评论查看";
}
<h2>评论查看</h2>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VideoId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vname)
        </th>
        <th>
            @Html.DisplayNameFor(model=>model.Content)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CommentTime)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VideoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vname)
            </td>
            <td>
                @Html.DisplayFor(modelItem=>item.Content)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CommentTime)
            </td>
        </tr>
    }

</table>


